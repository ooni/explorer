language: node_js
env:
  - REPO_NAME=explorer
node_js:
  - "node"
cache:
  directories:
    - "node_modules"
script: npm test
before_deploy: npm install now --no-save
deploy:
  - provider: script
    script: scripts/now-preview.sh
    skip_cleanup: true
    on:
      all_branches: true
